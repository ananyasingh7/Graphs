import java.util.ArrayList;
import java.util.HashSet;

class Graph{

    ArrayList<Node> listOfNodes;
    public Graph(){
        listOfNodes = new ArrayList<Node>();
    }

    void addNode(final String nodeVal){ //adds a new node to the Graph

    }

    void addUndirectedEdge(final Node first, final Node second){ //Adds an undirected edge between first and second

    }

    void removeUndirectedEdge(final Node first, final Node second){ //removes an edge between first and second

    }

    HashSet<Node> getAllNodes(){ //returns all the Nodes in the graph
        HashSet<Node> nodes = new HashSet<Node>();
        return nodes;
    }

    



}